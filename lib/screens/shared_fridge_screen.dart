import 'package:flutter/cupertino.dart';

class SharedFridgeScreen extends StatelessWidget {
  const SharedFridgeScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('プロフィール画面の内容'));
}